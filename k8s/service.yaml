apiVersion: v1
kind: Service
metadata:
  name: litellm-gateway
  namespace: default
spec:
  selector:
    app: litellm-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
